---
name: victim
namespace: victim
image: kieranajp/victim
version: latest
port: 3000

ingress:
  domain: example.com
  path: /victim

replicas: 1

# imagePullSecret: docker-registry-pullsecret

secrets:
- SLACK_APP_TOKEN
- SLACK_BOT_TOKEN
