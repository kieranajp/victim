---
name: victim
namespace: victim
image: kieranajp/victim
version: latest
port: 3000

ingress:
  domain: example.com
  path: /victim

replicas: 1

# imagePullSecret: docker-registry-pullsecret

database:
  host: 127.0.0.1
  port: 5432

secrets:
- SLACK_CLIENT_ID
- SLACK_CLIENT_SECRET
- DB_USER
- DB_PASS
